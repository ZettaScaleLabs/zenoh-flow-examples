flow: VideoPipeline
operators: []
sources:
  - id : Camera
    descriptor: ./face-detection/face-detection.yml
sinks:
  - id : Window
    descriptor: ./video-sink/video-sink.yml

links:
- from:
    node : Camera
    output : Frame
  to:
    node : Window
    input : Frame
